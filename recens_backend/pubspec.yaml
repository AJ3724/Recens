# name: recens_backend
# description: Recens Flutter app backend server (Dart/Shelf)
# version: 1.0.0

# environment:
#   sdk: ">=3.0.0 <4.0.0"

# dependencies:
#   shelf: ^1.4.1
#   shelf_router: ^1.1.4
#   # mysql_client: ^0.0.27
#   dotenv: ^4.2.0
#   mysql_client: ^0.2.1

# dev_dependencies:
#   lints: ^3.0.0

name: recens_backend
description: Recens Flutter app backend server (Dart/Shelf)
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  dotenv: ^4.2.0
  mysql_client: 0.0.27

dev_dependencies:
  lints: ^3.0.0